<!-- Generated by app/scripts/check-coverage-ledger.mts. Do not edit: run `npm run check:coverage -- --write`. -->

# Coverage ledger

Every one of the 184 requirements in the catalogue, and the thing that answers it.

This document exists to be disagreed with. An assessment tool that reports a score over some
unstated subset of a framework is asking to be trusted; this says which requirements it reads for
itself, which it asks a person about, and why each one is in the group it is in. A reader who
thinks an answer of the second kind is weaker evidence than one of the first is right, and can see
from the table below exactly which requirements that applies to.

It is generated from the catalogue, the resolver registry, the attestation questions and the
administrator evidence script's own manifest, and CI fails when it drifts from them. Nothing here
is maintained by hand, because a hand-maintained coverage claim is wrong within a month and
nobody finds out.

## Every requirement has an answer path

| How it is answered | Requirements | What that means |
| --- | --- | --- |
| **Measured** | 85 | The app reads the platform and reaches a verdict. No person is asked. |
| **Question — setting** | 53 | Configuration this app could read and no install of it may. A check is written and cannot be authorised to run anywhere, so a person reads the screen instead. ADR 0016. |
| **Question — practice** | 46 | A practice no telemetry reaches for anybody — whether a runbook is rehearsed, whether a review happens. A person describes it, and is asked again on a cadence. |
| **Total** | **184** | Every entry in the catalogue. 165 of them are scored — the rest are the same requirement filed under a second pillar, and the next section says why both halves still need an answer. |

There is no fourth row, and that is the claim. A requirement with no check, no question and no plan
would report to a reader as an unmeasured gap belonging to nobody; the generator refuses to write
this file while one exists.

### Why this counts differently from the README

The README says **117 of 165 scored controls are automatable**, and this document says
**85 of 184 are measured**. Both are true, and the gap between them is where the honest
part of this tool lives, so it is worth four sentences rather than a footnote.

**184 against 165** is the alias collapse. 19 entries are one requirement filed under two
pillars — Delta history retention is a cost concern and a recovery concern — and a score counts each
once so overlap cannot inflate it. This ledger enumerates entries rather than scored requirements,
because every entry has to have an answer and both halves are read by somebody. Every count below is
over entries, so the README's 117 is 136 here.

**Declared automatable is a statement, not a capability.** 136 requirements say a machine could
answer them. That is a property of the requirement, written when the catalogue was authored, and it
is what the README counts.

**103 have a resolver**, and the two sets are not nested, which is the part a subtraction would
get wrong. 35 requirements are declared automatable with no resolver — and not one of them is a
backlog item: every one names an account-plane endpoint or a workspace scope no Databricks App is
granted, so the check cannot be written to run. Meanwhile 2 have a resolver without being
declared automatable, answered from signals collected for something else.

**85 are measured in an install**, because 18 of those 103 resolvers need a scope no install is
offered either. They are written and correct and cannot be authorised to run anywhere (ADR 0016), so
they appear below under **Question — setting** rather than under Measured. Counting them as coverage
would be counting effort. They and the 35 above are the reason the administrator evidence
script exists.

## What the administrator evidence script does for the requirements no install can read

The 53 above are the requirements a Databricks App cannot be granted the scope to read (ADR 0016). An
administrator can run `config/evidence/collect-evidence.py` under their own authority and import the
result, which turns some of those questions back into readings. Three states, because they are three
different amounts of progress and reporting them as one number would overstate the first:

| Administrator script | Requirements | What a reader gets today |
| --- | --- | --- |
| Imported | 34 | The script collects it and the app revives it into a finding. Evidence, marked as administrator-collected rather than observed. |
| Collected, held | 16 | The script collects it and the app does not yet read it back. Held and named rather than discarded, so the import is not lossy — but the answer is still the person's. |
| Not collected | 3 | Neither collected nor revived. The question is the whole answer. |

## Why every question is a question

The 46 practice questions above are the expensive answers: each costs a person's attention and
buys an answer no better than their word. So each one records what a machine would have to observe
and whether anything records it, and the verdict that follows is published here rather than kept
where only the catalogue can see it. The uncomfortable row is the third one.

| Verdict | Questions | What it means |
| --- | --- | --- |
| Beyond telemetry | 10 | Nothing the platform records bears on the answer. A person is the only source there is. |
| Partial telemetry | 36 | Something is recorded, it narrows the answer, and it does not settle it. The question stands and names what bears on it. |
| Owed a measure | 0 | The platform records enough to answer this and the app does not read it yet. A debt, not a design. |

**0 of the 46 questions should not be questions.** That is the number this section exists to
publish. Each one below names the signal that would answer it, so the claim can be checked rather
than taken on trust — and so that a question standing in for a measurement cannot quietly become
permanent, which is what happens when the only record of the compromise is that somebody once knew.

### Owed a measure

The platform records enough to answer these. Until each is read, a person is asked instead.

None remain.

### Partial telemetry

Something is recorded and it is not enough. The question stands, and names what bears on it so a
reader can judge how much of the answer is really the person’s.

- **`CO-01-07`** Use the right instance type
  Node types are recorded per cluster and per-node CPU and memory are recorded alongside them, so a family mismatched to the load it carries is visible. Whether the family was chosen for the workload class or inherited is a fact about a decision, and no table holds it.
  Bears on it: `system.compute.clusters.worker_node_type with system.compute.node_timeline`.
- **`CO-01-09`** Right-size compute resources at deployment
  A cluster’s size at creation and its utilisation over the days after are both recorded, so sizing generously and leaving it shows up without anybody being asked. Whether a sizing figure was required at review is a property of a process the platform does not see.
  Bears on it: `system.compute.clusters.create_time with system.compute.node_timeline`.
- **`CO-03-03`** Monitor costs to align spending with expectations
  Reads of the billing tables are recorded like any other query, so whether anybody looks at spend, and how regularly, is measurable. Whether the comparison has consequences is the half the question is really for, and nothing records a decision that followed a dashboard.
  Bears on it: `system.query.history filtered to reads of system.billing`.
- **`CO-03-04`** Manage costs to align usage with organizational needs
  A warehouse downsized, a schedule reduced or a job retired are all recorded as changes with timestamps, so the route existing is observable. That cost was the reason is not: the platform records the edit and never the motive.
  Bears on it: `system.compute.warehouses change history with system.lakeflow.jobs.paused and delete_time`.
- **`DG-01-01`** Establish a data and AI governance process
  An owner is recorded on every catalogue asset, so whether new assets get one is measurable and is the part of this that bites. Whether a written process exists and work is routed through it is an organisational fact with no telemetry.
  Bears on it: `system.information_schema.tables.table_owner`.
- **`DG-02-01`** Centralize access control for all data and AI assets
  Unity Catalog grants are visible. The two common bypasses are not: a legacy metastore is invisible to every census this app runs, and grants made directly in cloud IAM are outside the platform, so no reading here can prove the absence of either.
  Bears on it: `system.information_schema.table_privileges, for the grants the catalogue can see`.
- **`DG-03-01`** Define clear data quality standards
  A declared constraint is the machine-readable form of an expectation, and a CHECK constraint is readable — as a `delta.constraints.*` table property, which this app already reads for the tables it samples. Not from the information schema: `check_constraints` carries the right columns and stays empty, verified by declaring one on a labs table and finding it in the properties and in neither constraint view. An expectation agreed with a consumer and written in a document rather than declared on the table is invisible either way, and is what this asks about.
  Bears on it: `DESCRIBE DETAIL properties, delta.constraints.* (already read as describe:storage.table_details)`.
- **`IU-01-03`** Use certified partner tools
  Every statement records the application that sent it, so the tools in the data path are enumerable and the question should arrive with that list already filled in — `sql:workload.sql_paths` reads it. What the enumeration cannot do is the part the requirement turns on. Which of those tools hold a current Databricks certification is a list Databricks maintains and revises, and an app carrying its own copy would answer from a stale one; the first audit of this said the app could hold that list, which was wrong in the direction that matters, because a confidently wrong certification claim is worse than a question.
  Bears on it: `system.query.history.client_application`.
- **`IU-01-04`** Reduce complexity of data engineering pipelines
  Lineage records how many hops a dataset passes through and how that has moved, so accretion is visible as a trend. Whether a simplification was made deliberately in the last year is a claim about intent that a shorter graph does not establish.
  Bears on it: `system.access.table_lineage`.
- **`IU-02-03`** Use open standards for developing agents, LLMs, and ML models
  Whether serving points at an external vendor model or at a portable artefact is recorded per served entity, which answers the concrete half. What a move away would cost is a judgement about code the platform does not hold.
  Bears on it: `system.serving.served_entities.external_model_config and foundation_model_config`.
- **`IU-03-01`** Provide a self-service experience across the platform
  Who creates compute and catalogue objects is audited, so whether teams provision for themselves or a central few do it for everybody is visible. How long a ticket takes, and whether one is needed at all, is not recorded.
  Bears on it: `system.access.audit create actions by principal`.
- **`OE-01-03`** Standardize DevOps processes (CI/CD)
  Deployment provenance shows code arrived by a deploy rather than by hand, which is the observable half. Whether review and tests ran before that deploy happened in a pipeline outside the platform, and nothing here records it.
  Bears on it: `system.lakeflow.jobs.deployment`.
- **`OE-01-05`** Define environment isolation strategy
  Grants held by a development identity on production catalogues are recorded, so the catalogue half of isolation is measurable. Isolation at the network and cloud account layer is outside a workspace’s view, and it is where the question’s worst case lives.
  Bears on it: `system.information_schema.table_privileges with system.access.audit`.
- **`OE-02-07`** Follow the deploy-code approach for ML workloads
  A served version that traces to a run recorded in this workspace, versus one that appears without any, separates promoted code from a copied binary in the common case. It does not do so reliably, because a legitimate promotion can carry its run history with it.
  Bears on it: `system.mlflow.runs_latest with system.serving.served_entities.entity_version`.
- **`OE-02-10`** Reuse the same infrastructure to manage ML pipelines
  ML work running as jobs and pipelines on the platform’s own compute is recorded and countable. Work running on separately maintained infrastructure is invisible precisely because it is elsewhere, so an empty result cannot distinguish good practice from a blind spot.
  Bears on it: `system.lakeflow.jobs and pipelines with system.compute.clusters`.
- **`PE-03-01`** Understand your data ingestion and access patterns
  How the largest datasets are read is recorded in detail — partitions touched, files pruned, bytes scanned, how often. Whether the team knows any of that is a different claim, and it is the one asked.
  Bears on it: `system.query.history.read_partitions, pruned_files and read_bytes`.
- **`PE-03-02`** Use parallel computation where it is beneficial
  The task graph records where a job runs stages in sequence with no dependency between them, which is the common form of this problem. Parallelism inside a single task is invisible, so a clean graph does not settle the answer.
  Bears on it: `system.lakeflow.job_tasks.depends_on_keys`.
- **`PE-03-04`** Prefer larger clusters
  Cluster size, node utilisation and run duration together show whether heavy jobs are under-provisioned, which is the substance. Whether a larger size was actually tried and compared on cost is an experiment, and an untried option leaves no trace.
  Bears on it: `system.compute.clusters.worker_count with node_timeline and job_task_run_timeline`.
- **`PE-03-07`** Use native Spark operations
  A UDF registered in Unity Catalog is catalogued with the language it is written in, so Python functions callable from SQL are enumerable — four of them on the labs workspace. That is one of the two shapes this warns about. The other happens inside notebook Python, where a driver-side loop over a collected DataFrame is a property of code the platform neither stores nor parses, and no count of registered functions reaches it.
  Bears on it: `system.information_schema.routines.external_language`.
- **`PE-03-09`** Understand your hardware and workload type
  Whether a workload is memory-bound or CPU-bound is recorded as utilisation, and the hardware it was given is recorded beside it, so a mismatch is visible. Whether the match was intended is not, and one family used for everything can still fit everything by luck.
  Bears on it: `system.compute.node_timeline with system.compute.clusters node types`.
- **`PE-03-14`** Optimize join performance
  Shuffle volume and local spill are recorded per statement and mark joins going badly, which finds the problem. Join strategy and skew live in the query profile, which has no system table, so the diagnosis the question asks for cannot be completed from telemetry.
  Bears on it: `system.query.history.shuffle_read_bytes and spilled_local_bytes`.
- **`PE-04-02`** Consider prewarming resources
  The cold start is timed rather than inferred: every statement records how long it waited for compute, and the warehouse event log records the start that made it wait. So a workload whose first run of the day pays a start-up penalty is identifiable. What is not recorded is the deadline — nothing says which workloads have one — so a penalty cannot be told from an acceptable wait, and warm compute cannot be told from compute that happened to still be up.
  Bears on it: `system.query.history.waiting_for_compute_duration_ms with system.compute.warehouse_events`.
- **`PE-05-01`** Configure performance monitoring at deployment
  Health rules are recorded on a job with a change time, so monitoring added long after the job shipped is distinguishable from monitoring configured with it. Dashboards and monitoring built outside the platform are not recorded, and are common.
  Bears on it: `system.lakeflow.jobs.health_rules`.
- **`PE-05-02`** Monitor query performance
  Whether anybody reads query history, and whether alerts exist over it, is recorded. Whether a query getting slower would be noticed before a user complained is a claim about attention that no signal settles.
  Bears on it: `system.query.history with system.access.audit for alert use`.
- **`REL-01-03`** Automatically rescue invalid or nonconforming data
  Pipeline expectations and what they do on violation are recorded where pipelines are used, which answers it for that estate. Rescue logic hand-written in a notebook is not recorded, and the silent-vanish case the question warns about is exactly the hand-written one. The table-constraint half is read from `delta.constraints.*` properties rather than from the information schema, which does not carry CHECK constraints.
  Bears on it: `system.lakeflow.pipelines.settings with DESCRIBE DETAIL properties`.
- **`REL-01-05`** Use scalable and production-grade model serving infrastructure
  Managed serving and its scaling configuration are recorded, so the infrastructure half is readable. Whether it survived an instance failure or a traffic spike is a fact about an event that may never have happened.
  Bears on it: `system.serving.served_entities with system.serving.endpoint_usage`.
- **`REL-02-01`** Use a layered storage architecture
  Lineage depth records whether data passes through distinct layers before it is served, which is the shape the question describes. Whether the layering is deliberate, and whether raw data is retained long enough to rerun from, are separate claims one graph does not answer.
  Bears on it: `system.access.table_lineage with system.information_schema.schemata`.
- **`REL-02-02`** Improve data integrity by reducing data redundancy
  Column-level lineage records where each column came from, so one source column landing in many tables is countable — 43,014 edges on the labs workspace. It narrows rather than settles, because a fan-out is what a well-built mart looks like as much as what a redundant copy looks like, and deciding which needs the meaning of the tables. A copy made by a job that read one table and wrote another without a declared derivation leaves no edge at all, and that is the case that causes the disagreement.
  Bears on it: `system.access.column_lineage`.
- **`REL-02-03`** Actively manage schemas
  Column additions and drops are audited, so schema movement is visible after it happens. Whether the change was detected and handled, or silently dropped a column that a consumer was reading, is the half that matters and is not recorded.
  Bears on it: `system.access.audit schema change operations`.
- **`REL-04-01`** Recover from Structured Streaming query failures
  A streaming task that failed and resumed is recorded, which is evidence the recovery path ran at least once. Whether it was tested deliberately, and whether the resume lost or duplicated records, is not something the run timeline can tell.
  Bears on it: `system.lakeflow.job_task_run_timeline`.
- **`REL-04-02`** Recover ETL jobs using data time travel capabilities
  A restore that happened is audited, so where the answer is yes there is a record of it. A no is indistinguishable from a workspace that has never needed one, which is why the question still has to be asked.
  Bears on it: `system.access.audit restore operations`.
- **`REL-04-03`** Leverage a job automation framework with built-in recovery
  Repeat attempts on a failed task are recorded, so retries that happened are visible. The configured retry policy is in no system table, and whether a resume was safe rather than merely successful is a property of the code.
  Bears on it: `system.lakeflow.job_task_run_timeline with system.lakeflow.job_tasks.timeout_seconds`.
- **`SCP-01-02`** Identity and access management best practices
  Whether a grant is held by a group or by a named individual is recorded on every securable, so the least-privilege half is measurable and ought to be measured. Whether access is removed when somebody moves on needs the joiner-mover-leaver process, which is not in the platform.
  Bears on it: `system.information_schema.table_privileges, schema_privileges and catalog_privileges`.
- **`SCP-03-02`** Network security best practices
  The first clause is read already. `enableIpAccessLists` is one of the fifteen keys this app asks `workspace-conf` for on every scan, and it settles SCP-03-10 — so the claim that the scope is unavailable was wrong about the setting this very question opens with. What is left is the egress half: serverless egress is logged, but that table is empty on a workspace that constrains nothing and on one that logs nothing, and the cloud-side network design behind it is outside the platform.
  Bears on it: `workspace-conf enableIpAccessLists, already read for SCP-03-10, with system.access.outbound_network`.
- **`SCP-03-13`** Egress control from compute — live connectivity test
  The experiment is not recorded — its result exists only if somebody ran it — but the thing the experiment is trying to establish partly is: serverless egress is logged, so a destination compute actually reached is visible. It narrows rather than settles for two reasons. The table is empty on a workspace whose egress is genuinely constrained and on one where the logging is not enabled, and it covers serverless compute rather than the classic cluster the question asks about.
  Bears on it: `system.access.outbound_network`.
- **`SCP-04-06`** DBFS mounts
  Mount and unmount are audited, so mounts created within the retained window are visible. A mount made before it and still in use is not, because listing what is mounted now needs a command run on compute.
  Bears on it: `system.access.audit mount operations`.

### Beyond telemetry

No signal bears on these, and none is expected to: they are facts about people, plans and
intentions rather than about the estate. This is the claim most worth disagreeing with, so each one
says what a machine would have had to see.

- **`CO-03-02`** Set up budgets and alerts to enable monitoring of account spending
  Budgets are account-plane objects. Checked rather than assumed: `system.billing` holds four tables — usage, attributed usage, list prices and account prices — and no budget among them, and `/api/2.1/budget-policies` answers Not Found from a workspace. The part that decides the answer is further out still: whether an alert reached somebody who acted on it is not recorded by anything.
- **`IU-01-01`** Use standard and reusable integration patterns for external integration
  The platform records the integration that exists, never whether it followed an established pattern or was designed from scratch again. Reuse is a fact about how a design was arrived at, and two identical connections can differ entirely on it.
- **`OE-01-01`** Create a dedicated Databricks operations team
  Whether a team exists and what it is accountable for is an organisational fact. No table records reporting lines, and a workspace with one attentive engineer looks identical to a workspace with a staffed platform team.
- **`OE-03-02`** Invest in capacity planning
  A forward view is a plan. Growth and seasonality are measurable in arrears from billing, but whether anybody looked ahead and produced a number is not something the estate records.
- **`PE-03-03`** Analyze the whole chain of execution
  This asks how people diagnose a problem — whether they look at the whole chain or reach for the Spark job. The platform records the workload and never the method used to look at it.
- **`PE-04-01`** Test on data representative of production data
  This compares test data against production data, and the platform records no notion of which estate is which. Volumes are visible; deciding that one catalogue is the test one requires a naming convention the platform neither knows nor enforces.
- **`PE-04-03`** Identify bottlenecks
  Asks where a diagnosis started: evidence or expectation. That is a fact about how people work, and the estate looks the same either way.
- **`REL-02-05`** Take a data-centric approach to machine learning
  Asks what an investigation started with — the data or the model. A sequence of human decisions, leaving no mark on the estate either way.
- **`REL-04-04`** Configure high availability and disaster recovery
  A plan, a stated recovery objective, and an exercise. None of the three is an estate fact, and a workspace that has rehearsed failover looks exactly like one that intends to.
- **`SCP-03-01`** Network deployment considerations for AWS
  Network topology is cloud-plane configuration with no workspace-readable source, and the question is about whether a design was chosen rather than accepted — which nothing records even where the topology is visible.

## By pillar

| Pillar | Requirements | Measured | Question — setting | Question — practice |
| --- | --- | --- | --- | --- |
| Cost optimization | 22 | 17 | 0 | 5 |
| Data and AI governance | 13 | 10 | 0 | 3 |
| Interoperability and usability | 15 | 10 | 0 | 5 |
| Operational excellence | 21 | 15 | 0 | 6 |
| Performance efficiency | 25 | 13 | 0 | 12 |
| Reliability | 18 | 8 | 0 | 10 |
| Security, compliance, and privacy | 70 | 12 | 53 | 5 |

## What may produce a verdict

A second axis, and a different question from the one above. **How it is answered** says where the
evidence comes from today. **Judgment route** says what may ever reach a conclusion from it — and it
exists so that eligibility for model-assisted judgment is settled once, here, rather than argued
control by control at the point somebody writes the call.

| Route | Requirements | What it means |
| --- | --- | --- |
| **Deterministic** | 85 | A resolver answers it from readings, or the reading is owed and the debt is paid with the reading. Nothing is sent anywhere. |
| **LLM-eligible** | 29 | Somebody wrote down what a rubric would weigh that the reading does not settle. Eligible is not scored: no model verdict enters the published assessment. |
| **Evidence-incomplete** | 68 | Nothing collected bears on a verdict — either no install may read it, or something narrows it and no rubric has been authored over what that is. |
| **Human-accountable** | 2 | Nothing recorded bears on it and no rubric claims otherwise, so the answer is a person’s. |

Three of the four are derived from the catalogue and the registry. `llm-eligible` is not, and cannot
be: the catalogue records that a reading exists for a requirement and never whether that reading is a
number to compute or evidence to read, which is the whole of the route. So it is authored per
requirement with a reason, and the default is not eligible — a `partial-telemetry` requirement nobody
has written a reason for stays `evidence-incomplete`.

**8 of the 29 would be judged over declarations alone.** Nothing recorded bears on them, so a verdict inherits the authority of what somebody said and cannot exceed it. They are marked below and the routing gate holds that label to the catalogue’s own verdict: `IU-01-01`, `OE-03-02`, `PE-03-03`, `PE-04-01`, `PE-04-03`, `REL-02-05`, `REL-04-04`, `SCP-03-01`.

## Every requirement

Ordered by requirement id. **What answers it** names the signals a measured requirement rests on, the
endpoint a setting question is about, or the review cadence of a practice question — so a reader can
check a row rather than only count it.

| Requirement | Pillar | How it is answered | What answers it | Judgment route |
| --- | --- | --- | --- | --- |
| `CO-01-01` | CO | Measured | `sql:uc.census` | Deterministic |
| `CO-01-02` | CO | Measured | `sql:cost.compute_mix` | Deterministic |
| `CO-01-03` | CO | Measured | `sql:workload.sql_paths` | Deterministic |
| `CO-01-04` | CO | Measured | `sql:compute.clusters` | Deterministic |
| `CO-01-05` | CO | Measured | `sql:compute.clusters` | Deterministic |
| `CO-01-06` | CO | Measured | `sql:cost.compute_mix` | Deterministic |
| `CO-01-07` | CO | Question — practice | every 365 days | Evidence-incomplete |
| `CO-01-08` | CO | Measured | `sql:compute.node_utilization` | Deterministic |
| `CO-01-09` | CO | Question — practice | every 365 days | Evidence-incomplete |
| `CO-01-10` | CO | Measured | `sql:cost.compute_mix` | Deterministic |
| `CO-02-01` | CO | Measured | `sql:compute.clusters`, `sql:compute.warehouses` | Deterministic |
| `CO-02-02` | CO | Measured | `sql:compute.clusters`, `sql:compute.warehouses` | Deterministic |
| `CO-02-03` | CO | Measured | `sql:compute.clusters` | Deterministic |
| `CO-03-01` | CO | Measured | `sql:cost.attribution` | Deterministic |
| `CO-03-02` | CO | Question — practice | every 180 days | Human-accountable |
| `CO-03-03` | CO | Question — practice | every 180 days | Evidence-incomplete |
| `CO-03-04` | CO | Question — practice | every 180 days | Evidence-incomplete |
| `CO-03-05` | CO | Measured | `sql:storage.table_metrics`, `describe:storage.table_details` | Deterministic |
| `CO-03-06` | CO | Measured | `sql:maintenance.recency`, `describe:predictive_optimization.coverage` | Deterministic |
| `CO-03-07` | CO | Measured | `describe:storage.table_details` | Deterministic |
| `CO-04-01` | CO | Measured | `sql:jobs.inventory` | Deterministic |
| `CO-04-02` | CO | Measured | `sql:compute.clusters` | Deterministic |
| `DG-01-01` | DG | Question — practice | every 365 days | LLM-eligible |
| `DG-01-02` | DG | Measured | `sql:uc.census` | Deterministic |
| `DG-01-03` | DG | Measured | `sql:uc.census` | Deterministic |
| `DG-01-04` | DG | Measured | `sql:uc.lineage_coverage` | Deterministic |
| `DG-01-05` | DG | Measured | `sql:uc.census` | Deterministic |
| `DG-01-06` | DG | Measured | `sql:uc.discovery` | Deterministic |
| `DG-01-07` | DG | Measured | `sql:uc.census` | Deterministic |
| `DG-02-01` | DG | Question — practice | every 90 days | Evidence-incomplete |
| `DG-02-02` | DG | Measured | `sql:governance.audit_coverage` | Deterministic |
| `DG-02-03` | DG | Measured | `sql:governance.audit_coverage` | Deterministic |
| `DG-03-01` | DG | Question — practice | every 365 days | LLM-eligible |
| `DG-03-02` | DG | Measured | `sql:uc.quality_monitoring` | Deterministic |
| `DG-03-03` | DG | Measured | `sql:uc.census` | Deterministic |
| `IU-01-01` | IU | Question — practice | every 365 days | LLM-eligible (declarations only) |
| `IU-01-02` | IU | Measured | `sql:uc.platform_census` | Deterministic |
| `IU-01-03` | IU | Question — practice | every 365 days | Evidence-incomplete |
| `IU-01-04` | IU | Question — practice | every 365 days | LLM-eligible |
| `IU-01-05` | IU | Measured | `sql:jobs.inventory`, `sql:pipelines.inventory` | Deterministic |
| `IU-02-01` | IU | Measured | `sql:uc.census` | Deterministic |
| `IU-02-02` | IU | Measured | `sql:uc.platform_census` | Deterministic |
| `IU-02-03` | IU | Question — practice | every 365 days | LLM-eligible |
| `IU-03-01` | IU | Question — practice | every 365 days | Evidence-incomplete |
| `IU-03-02` | IU | Measured | `sql:cost.compute_mix` | Deterministic |
| `IU-03-03` | IU | Measured | `sql:compute.clusters` | Deterministic |
| `IU-03-04` | IU | Measured | `rest:workspace:serving-endpoints`, `rest:workspace:vector-search.endpoints` | Deterministic |
| `IU-04-01` | IU | Measured | `sql:uc.platform_census`, `sql:uc.census` | Deterministic |
| `IU-04-02` | IU | Measured | `sql:uc.platform_census`, `sql:uc.census` | Deterministic |
| `IU-04-03` | IU | Measured | `sql:uc.census`, `sql:uc.lineage_coverage` | Deterministic |
| `OE-01-01` | OE | Question — practice | every 365 days | Human-accountable |
| `OE-01-02` | OE | Measured | `sql:jobs.inventory`, `sql:pipelines.inventory` | Deterministic |
| `OE-01-03` | OE | Question — practice | every 180 days | LLM-eligible |
| `OE-01-04` | OE | Measured | `sql:serving.model_entities`, `sql:mlflow.run_tracking` | Deterministic |
| `OE-01-05` | OE | Question — practice | every 90 days | LLM-eligible |
| `OE-01-06` | OE | Measured | `sql:uc.census` | Deterministic |
| `OE-02-01` | OE | Measured | `sql:jobs.inventory`, `sql:pipelines.inventory` | Deterministic |
| `OE-02-02` | OE | Measured | `sql:compute.clusters` | Deterministic |
| `OE-02-03` | OE | Measured | `sql:uc.census` | Deterministic |
| `OE-02-04` | OE | Measured | `sql:jobs.inventory` | Deterministic |
| `OE-02-05` | OE | Measured | `sql:jobs.inventory` | Deterministic |
| `OE-02-06` | OE | Measured | `sql:pipelines.inventory`, `sql:jobs.inventory` | Deterministic |
| `OE-02-07` | OE | Question — practice | every 365 days | LLM-eligible |
| `OE-02-08` | OE | Measured | `sql:serving.model_entities` | Deterministic |
| `OE-02-09` | OE | Measured | `sql:mlflow.run_tracking` | Deterministic |
| `OE-02-10` | OE | Question — practice | every 365 days | LLM-eligible |
| `OE-02-11` | OE | Measured | `sql:pipelines.inventory`, `sql:jobs.inventory` | Deterministic |
| `OE-03-01` | OE | Measured | `sql:query.capacity` | Deterministic |
| `OE-03-02` | OE | Question — practice | every 180 days | LLM-eligible (declarations only) |
| `OE-04-01` | OE | Measured | `sql:jobs.inventory` | Deterministic |
| `OE-04-02` | OE | Measured | `sql:jobs.inventory` | Deterministic |
| `PE-02-01` | PE | Measured | `sql:cost.compute_mix` | Deterministic |
| `PE-02-02` | PE | Measured | `sql:serving.model_entities` | Deterministic |
| `PE-03-01` | PE | Question — practice | every 180 days | Evidence-incomplete |
| `PE-03-02` | PE | Question — practice | every 365 days | Evidence-incomplete |
| `PE-03-03` | PE | Question — practice | every 180 days | LLM-eligible (declarations only) |
| `PE-03-04` | PE | Question — practice | every 365 days | Evidence-incomplete |
| `PE-03-05` | PE | Measured | `describe:predictive_optimization.coverage` | Deterministic |
| `PE-03-06` | PE | Measured | `sql:uc.census` | Deterministic |
| `PE-03-07` | PE | Question — practice | every 365 days | LLM-eligible |
| `PE-03-08` | PE | Measured | `sql:cost.compute_mix` | Deterministic |
| `PE-03-09` | PE | Question — practice | every 365 days | LLM-eligible |
| `PE-03-10` | PE | Measured | `sql:workload.sql_paths` | Deterministic |
| `PE-03-11` | PE | Measured | `sql:maintenance.recency`, `describe:predictive_optimization.coverage`, `sql:storage.table_metrics`, `describe:storage.table_details` | Deterministic |
| `PE-03-12` | PE | Measured | `describe:storage.table_details` | Deterministic |
| `PE-03-13` | PE | Measured | `describe:storage.table_details` | Deterministic |
| `PE-03-14` | PE | Question — practice | every 365 days | LLM-eligible |
| `PE-03-15` | PE | Measured | `sql:maintenance.recency`, `describe:predictive_optimization.coverage` | Deterministic |
| `PE-03-16` | PE | Measured | `describe:storage.table_details` | Deterministic |
| `PE-04-01` | PE | Question — practice | every 365 days | LLM-eligible (declarations only) |
| `PE-04-02` | PE | Question — practice | every 365 days | Evidence-incomplete |
| `PE-04-03` | PE | Question — practice | every 180 days | LLM-eligible (declarations only) |
| `PE-05-01` | PE | Question — practice | every 180 days | Evidence-incomplete |
| `PE-05-02` | PE | Question — practice | every 180 days | Evidence-incomplete |
| `PE-05-03` | PE | Measured | `sql:jobs.inventory` | Deterministic |
| `PE-05-04` | PE | Measured | `sql:jobs.inventory` | Deterministic |
| `REL-01-01` | REL | Measured | `sql:uc.census` | Deterministic |
| `REL-01-02` | REL | Measured | `sql:compute.clusters` | Deterministic |
| `REL-01-03` | REL | Question — practice | every 180 days | LLM-eligible |
| `REL-01-04` | REL | Measured | `sql:jobs.inventory` | Deterministic |
| `REL-01-05` | REL | Question — practice | every 180 days | Evidence-incomplete |
| `REL-01-06` | REL | Measured | `sql:cost.compute_mix` | Deterministic |
| `REL-02-01` | REL | Question — practice | every 365 days | LLM-eligible |
| `REL-02-02` | REL | Question — practice | every 365 days | LLM-eligible |
| `REL-02-03` | REL | Question — practice | every 180 days | LLM-eligible |
| `REL-02-04` | REL | Measured | `describe:storage.table_details` | Deterministic |
| `REL-02-05` | REL | Question — practice | every 365 days | LLM-eligible (declarations only) |
| `REL-03-01` | REL | Measured | `sql:compute.clusters`, `sql:compute.warehouses` | Deterministic |
| `REL-03-02` | REL | Measured | `sql:compute.warehouses` | Deterministic |
| `REL-04-01` | REL | Question — practice | every 90 days | LLM-eligible |
| `REL-04-02` | REL | Question — practice | every 90 days | LLM-eligible |
| `REL-04-03` | REL | Question — practice | every 180 days | LLM-eligible |
| `REL-04-04` | REL | Question — practice | every 90 days | LLM-eligible (declarations only) |
| `REL-04-05` | REL | Measured | `describe:storage.table_details` | Deterministic |
| `SCP-01-01` | SCP | Measured | `sql:security.auth_login_paths` | Deterministic |
| `SCP-01-02` | SCP | Question — practice | every 90 days | LLM-eligible |
| `SCP-01-03` | SCP | Question — setting (imported) | `rest:workspace:token.list` | Evidence-incomplete |
| `SCP-01-04` | SCP | Question — setting (imported) | `rest:workspace:preview.workspace-conf` | Evidence-incomplete |
| `SCP-01-05` | SCP | Question — setting (imported) | `rest:workspace:token.list` | Evidence-incomplete |
| `SCP-01-06` | SCP | Question — setting (imported) | `rest:workspace:permissions.authorization.tokens` | Evidence-incomplete |
| `SCP-01-07` | SCP | Question — setting (not collected) | `rest:account:accounts.servicePrincipals.credentials.secrets` | Evidence-incomplete |
| `SCP-02-01` | SCP | Question — setting (imported) | `rest:workspace:secrets.scopes.list` | Evidence-incomplete |
| `SCP-02-02` | SCP | Question — setting (imported) | `rest:workspace:clusters.list` | Evidence-incomplete |
| `SCP-02-03` | SCP | Question — setting (collected, held) | `rest:account:accounts.workspaces` | Evidence-incomplete |
| `SCP-02-04` | SCP | Question — setting (imported) | `rest:workspace:preview.workspace-conf` | Evidence-incomplete |
| `SCP-02-05` | SCP | Question — setting (imported) | `rest:workspace:preview.workspace-conf` | Evidence-incomplete |
| `SCP-02-06` | SCP | Question — setting (imported) | `rest:workspace:preview.workspace-conf` | Evidence-incomplete |
| `SCP-02-07` | SCP | Question — setting (imported) | `rest:workspace:preview.workspace-conf` | Evidence-incomplete |
| `SCP-02-08` | SCP | Question — setting (imported) | `rest:workspace:preview.workspace-conf` | Evidence-incomplete |
| `SCP-02-09` | SCP | Measured | `rest:workspace:vector-search.endpoints` | Deterministic |
| `SCP-02-10` | SCP | Question — setting (imported) | `rest:workspace:settings.types.disable_legacy_dbfs.names.default` | Evidence-incomplete |
| `SCP-02-11` | SCP | Question — setting (imported) | `rest:workspace:settings.types.sql_results_download.names.default` | Evidence-incomplete |
| `SCP-02-12` | SCP | Question — setting (imported) | `rest:workspace:preview.workspace-conf` | Evidence-incomplete |
| `SCP-03-01` | SCP | Question — practice | every 90 days | LLM-eligible (declarations only) |
| `SCP-03-02` | SCP | Question — practice | every 90 days | LLM-eligible |
| `SCP-03-03` | SCP | Question — setting (collected, held) | `rest:account:accounts.workspaces` | Evidence-incomplete |
| `SCP-03-04` | SCP | Question — setting (collected, held) | `rest:account:accounts.workspaces` | Evidence-incomplete |
| `SCP-03-05` | SCP | Question — setting (imported) | `rest:workspace:ip-access-lists` | Evidence-incomplete |
| `SCP-03-06` | SCP | Question — setting (collected, held) | `rest:workspace:accounts.workspaces` | Evidence-incomplete |
| `SCP-03-07` | SCP | Measured | `rest:workspace:serving-endpoints` | Deterministic |
| `SCP-03-08` | SCP | Question — setting (imported) | `rest:account:accounts.{account_id}.ip-access-lists` | Evidence-incomplete |
| `SCP-03-09` | SCP | Question — setting (collected, held) | `rest:account:accounts.network-policies` | Evidence-incomplete |
| `SCP-03-10` | SCP | Question — setting (imported) | `rest:workspace:preview.workspace-conf` | Evidence-incomplete |
| `SCP-03-11` | SCP | Question — setting (collected, held) | `rest:account:accounts.network-policies` | Evidence-incomplete |
| `SCP-03-12` | SCP | Question — setting (imported) | `rest:account:accounts.{account_id}.ip-access-lists` | Evidence-incomplete |
| `SCP-03-13` | SCP | Question — practice | every 90 days | LLM-eligible |
| `SCP-04-01` | SCP | Question — setting (imported) | `rest:workspace:token.list` | Evidence-incomplete |
| `SCP-04-02` | SCP | Question — setting (imported) | `rest:account:accounts.log-delivery` | Evidence-incomplete |
| `SCP-04-03` | SCP | Question — setting (imported) | `rest:workspace:clusters.list` | Evidence-incomplete |
| `SCP-04-04` | SCP | Measured | `sql:compute.clusters` | Deterministic |
| `SCP-04-05` | SCP | Measured | `sql:security.dbfs_tables` | Deterministic |
| `SCP-04-06` | SCP | Question — practice | every 90 days | Evidence-incomplete |
| `SCP-04-07` | SCP | Measured | `sql:compute.clusters` | Deterministic |
| `SCP-04-08` | SCP | Question — setting (imported) | `rest:workspace:preview.workspace-conf` | Evidence-incomplete |
| `SCP-04-09` | SCP | Question — setting (imported) | `rest:workspace:preview.workspace-conf` | Evidence-incomplete |
| `SCP-04-10` | SCP | Measured | `sql:uc.census` | Deterministic |
| `SCP-04-11` | SCP | Question — setting (collected, held) | `rest:workspace:unity-catalog.metastore_summary` | Evidence-incomplete |
| `SCP-04-12` | SCP | Question — setting (collected, held) | `rest:workspace:unity-catalog.recipients` | Evidence-incomplete |
| `SCP-04-13` | SCP | Question — setting (collected, held) | `rest:workspace:unity-catalog.recipients` | Evidence-incomplete |
| `SCP-04-14` | SCP | Measured | `sql:uc.census` | Deterministic |
| `SCP-04-15` | SCP | Question — setting (collected, held) | `rest:workspace:unity-catalog.metastores` | Evidence-incomplete |
| `SCP-04-16` | SCP | Measured | `sql:compute.clusters` | Deterministic |
| `SCP-04-17` | SCP | Question — setting (collected, held) | `rest:workspace:unity-catalog.models` | Evidence-incomplete |
| `SCP-04-18` | SCP | Measured | `sql:governance.audit_coverage` | Deterministic |
| `SCP-04-19` | SCP | Question — setting (imported) | `rest:workspace:settings.types.restrict_workspace_admins.names.default` | Evidence-incomplete |
| `SCP-04-20` | SCP | Question — setting (imported) | `rest:workspace:settings.types.automatic_cluster_update.names.default` | Evidence-incomplete |
| `SCP-04-21` | SCP | Question — setting (imported) | `rest:account:accounts.settings.types.disable_legacy_features.names.default` | Evidence-incomplete |
| `SCP-04-22` | SCP | Measured | `sql:jobs.inventory` | Deterministic |
| `SCP-04-23` | SCP | Question — setting (not collected) | `rest:workspace:permissions.jobs.{job_id}` | Evidence-incomplete |
| `SCP-05-01` | SCP | Question — setting (collected, held) | `rest:workspace:libraries.all-cluster-statuses` | Evidence-incomplete |
| `SCP-05-02` | SCP | Question — setting (collected, held) | `rest:workspace:global-init-scripts` | Evidence-incomplete |
| `SCP-05-03` | SCP | Question — setting (collected, held) | `rest:workspace:preview.scim.v2.Groups` | Evidence-incomplete |
| `SCP-05-04` | SCP | Question — setting (imported) | `rest:workspace:preview.workspace-conf` | Evidence-incomplete |
| `SCP-05-05` | SCP | Question — setting (imported) | `rest:workspace:preview.workspace-conf` | Evidence-incomplete |
| `SCP-05-06` | SCP | Question — setting (imported) | `rest:workspace:preview.workspace-conf` | Evidence-incomplete |
| `SCP-05-07` | SCP | Question — setting (imported) | `rest:workspace:preview.workspace-conf` | Evidence-incomplete |
| `SCP-05-08` | SCP | Question — setting (collected, held) | `rest:workspace:unity-catalog.storage-credentials` | Evidence-incomplete |
| `SCP-05-09` | SCP | Question — setting (not collected) | `rest:workspace:unity-catalog.permissions.{securable_type}.{full_name}` | Evidence-incomplete |
| `SCP-05-10` | SCP | Measured | `rest:workspace:serving-endpoints` | Deterministic |
| `SCP-05-11` | SCP | Question — setting (imported) | `rest:account:accounts.settings.types.shield_csp_enablement_ac.names.default` | Evidence-incomplete |
| `SCP-05-12` | SCP | Question — setting (collected, held) | `rest:workspace:unity-catalog.artifact-allowlists.{artifact_type}` | Evidence-incomplete |
| `SCP-05-13` | SCP | Question — setting (imported) | `rest:workspace:settings.types.shield_csp_enablement_ws_db.names.default` | Evidence-incomplete |
| `SCP-05-14` | SCP | Question — setting (imported) | `rest:workspace:settings.types.shield_esm_enablement_ws_db.names.default` | Evidence-incomplete |
| `SCP-05-15` | SCP | Question — setting (imported) | `rest:workspace:preview.workspace-conf` | Evidence-incomplete |
