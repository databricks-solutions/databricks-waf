# Dependencies
node_modules

# Build outputs
dist
build
client/dist
.next
.databricks/

# Environment files
.env
.env.local
.env.*.local

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Coverage
coverage

# Cache
.cache
.turbo

# Lock files
package-lock.json
yarn.lock
pnpm-lock.yaml

# Vendor
vendor

# Generated by `appkit generate-types`; its shape depends on whether typegen
# could reach a warehouse, so formatting it produces spurious diffs.
shared/appkit-types

# Generated by `scripts/seed-catalogue.mjs`. The generator controls the layout,
# and reformatting would make every re-seed look like a change.
config/controls/*.yaml

# Generated and byte-checked by `scripts/methodology-manifest.mts`. Its digest is over the generated
# contract, and a formatter must not become a second writer of those bytes.
config/methodology/version-1.manifest.json

# Rewritten by `appkit plugin sync` on every build, so formatting it means the
# next build immediately un-formats it and CI fails on a file nobody edited.
appkit.plugins.json
