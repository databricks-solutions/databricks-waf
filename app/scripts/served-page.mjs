/**
 * A rendered application surface in the customer design system.
 *
 * The served drive is intentionally not coupled to a page name: every route counts when it paints a
 * customer surface, including utility and administrative records.
 */
export const RENDERED_SURFACE_SELECTOR = '.wa-customer-surface';
